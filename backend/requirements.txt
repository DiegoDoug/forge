fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
pydantic>=2.7,<3
pydantic-settings>=2.4,<3
sqlmodel>=0.0.21,<1
aiosqlite>=0.20,<1
greenlet>=3.0
alembic>=1.13,<2
pynacl>=1.5,<2
cryptography>=42,<44
pyjwt>=2.9,<3
croniter>=2.0,<3
qrcode[pil]>=7.4,<8

# Ingest document-to-markdown conversion (vendored service, see app/services/ingest)
markitdown[all]>=0.1.2

# Documents tab: multi-format export (see app/services/documents/export.py).
# python-docx, beautifulsoup4 and markdownify already ride in transitively via
# markitdown[all] — pinned here directly so export.py doesn't depend on that
# staying true across a markitdown upgrade.
python-docx>=1.1,<2
beautifulsoup4>=4.12,<5
markdownify>=0.13,<1
reportlab>=4.2,<5

# Optional vision-LLM PDF/image support for Ingest (safe to keep installed when disabled)
openai>=1.40,<2
pymupdf>=1.24,<2
