# $project_name — Agent Instructions

> Generated by Forge's Project Initialization Engine on $generated_date.

## Project summary

$description

## Tech stack

$tech_stack_bullets

## Conventions

$conventions_section

## Agent operating rules

- Read enough of the existing code to match its style before adding new code — do not introduce a second pattern for something the codebase already does one way.
- Treat any build, lint, or test command already configured in this repo as the source of truth for "done" — a change is not complete until those pass.
- Do not add new dependencies, delete data, or modify deployment/CI configuration without flagging it first.
- Prefer the smallest change that correctly satisfies the request over a larger, more "complete-looking" rewrite.
- Leave no TODO or placeholder code in a change presented as finished.
