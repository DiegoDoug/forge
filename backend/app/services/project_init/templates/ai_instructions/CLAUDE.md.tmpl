# $project_name

> Generated by Forge's Project Initialization Engine on $generated_date.

## What this project is

$description

## Tech stack

$tech_stack_bullets

## Conventions

$conventions_section

## Working with Claude Code in this repo

- Prefer editing existing files to creating new ones; match the conventions above rather than introducing new patterns.
- Ask before adding a new external dependency, changing the database schema, or touching CI/deployment configuration.
- Run this project's existing lint/typecheck/test commands before considering a change done, and fix any regressions immediately rather than leaving them for later.
- Keep changes scoped to what was asked — avoid speculative refactors or unrequested abstractions in the same change.
