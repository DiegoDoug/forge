# Universal Converter — Current State

> **Purpose:** Live snapshot of where this phase actually stands, updated at every checkpoint.
> **Scope:** This phase only — updated continuously, never left stale.
> **Ownership:** TODO — assign a phase owner.
> **Status:** 🔒 RELEASED & FROZEN — tagged `v0.4.0-universal-converter`, merged to `master` via [PR #19](https://github.com/DiegoDoug/forge/pull/19) (squash commit `8b5ee07`). Specification: Locked. Implementation: Closed. Future changes: bug fixes only.
> **Last Updated:** 2026-07-25

---


## Current Status

Design phase and Stage 4 milestone breakdown are complete (see below). The project owner reviewed `09_IMPLEMENTATION_TASKS.md` and **authorized implementation** (2026-07-23), unlocking Milestone 1, and set two standing rules for the remainder of implementation: (1) a project-owner review at every milestone boundary, not only at the end; (2) no milestone may introduce both an architectural change and a behavioral change simultaneously unless its stated objective explicitly requires both (see `09_IMPLEMENTATION_TASKS.md` §0.1 for the per-milestone classification this rule requires).

**Milestone 1 (Baseline characterization tests) is complete** — 27 new tests (`test_converters_cron.py`, `test_ingest_pipeline.py`), full suite 138 → 165 passed, 0 regressions, zero production code touched. Per this milestone's own classification (§0.1): neither architectural nor behavioral change, as expected. Approved by the project owner 2026-07-25.

**Milestone 2 (Provider interface and registry) is complete** — `backend/app/services/converters/providers/__init__.py` (new): `ConverterProvider` protocol, `register()`/`list_providers()`. 7 new tests in `test_converters_providers.py`, all against a throwaway fake provider only (no real pipeline wired yet — that's Milestone 3). Full suite: 165 → 172 passed, 0 regressions. Confirmed by AST inspection that the new module's only imports are `__future__` and `typing` — zero coupling to `services.ingest`, satisfying this milestone's architecture-only classification directly rather than by assertion. Approved by the project owner 2026-07-25.

**Milestone 3 (Ingest provider integration) is complete** — `providers/markitdown.py` (new, pure delegation to `services.ingest.converter.submit`), `schemas/converters.py` gained `ProviderOut`/`ProvidersOut`, `api/routes/converters.py` gained `GET /providers`. 11 new tests (2 extending `test_converters_providers.py`, 9 in new `test_converters_api.py` covering the new endpoint plus full contract preservation of every pre-existing `/api/ingest/*` route and `cron/parse`). Full suite: 172 → 183 passed, 0 regressions. `git diff --stat app/services/ingest/` confirmed empty — the wrapped pipeline is genuinely untouched. Two real issues found and fixed during this milestone: a test-isolation bug in the new tests themselves (a lazy import let a fixture swallow the one-time provider registration — fixed by importing at module level) and a pre-existing, Windows-only newline-translation detail in `converter.py`'s `write_text` call, exposed by reading raw response bytes instead of text (test adjusted to normalize before comparing, pipeline code untouched since it's out of scope and a no-op on the real Linux deployment target). Approved by the project owner 2026-07-25.

**Milestone 4 (Browser provider abstraction) is complete** — `frontend/features/converters/providers.ts` (new, ten-entry manifest — nine tool files, ten rendered components, since `url-unicode-tool.tsx` exports two). Zero existing tool files touched. Verified live via a throwaway page (deleted afterward): identical render to today's page, plus working interaction (JSON Minify, Cron's real backend call). Approved by the project owner 2026-07-25, who specifically noted the manifest correctly modeling all ten rendered tools and the live-interaction proof as evidence the abstraction is faithful, not just structurally valid.

**Milestone 5 (Unified Converter UI and routing) is complete** — the single `/converters` page now renders both sections; `/ingest` retired via a `next.config.ts` redirect (`307`, following the `/vault` alias precedent); `nav-registry.ts` consolidated to one "Converter" entry. A real, unplanned cross-feature dependency was found and fixed: Workbench's own "manually synced" nav-to-pinned-tool mapping had `"ingest"` as the *first* item in its default pinned-tools list and a forward-looking `"universal_converter"` placeholder anticipating this phase — both retired in favor of the single `"converters"` key, with one pre-existing test's hardcoded literal count updated to match. Full backend suite: 183 passed (unchanged count, one literal fixed), 0 regressions. Live-verified: single nav/palette entry, **Workbench's pinned-tools panel correctly showing a "Converter" tile** (direct proof the fix works), the redirect via both `window.location` and a raw `curl` check, zero network requests from a client-side tool, mobile/dark-mode checks. Approved by the project owner 2026-07-25.

**Milestone 6 (Cleanup, documentation, and regression hardening) is complete** — every `08_ACCEPTANCE.md` §1–§4 criterion is now checked against cited evidence (test names, live actions, or code inspection), not left as unverified release gates. Ran the full backend suite fresh (183 passed), confirmed zero new Alembic migrations and zero diff in the wrapped Ingest pipeline, and measured the new endpoint's real latency (0.21μs average) rather than asserting it. Ran the first fully continuous manual golden-path pass across one session, including a genuine file drop through the dropzone's file input (not just an API-level test) — the upload converged correctly after a real wait; an initial "stuck" appearance was traced to this environment's background-tab timer throttling, not a defect. Updated `docs/Architecture.md`, `docs/API.md`, `docs/FolderStructure.md`, root `README.md`, and `forge-docs/02_ROADMAP.md` (both of which were stale at "Not started"). Dead-code sweep found nothing left over from the old two-page structure. Approved by the project owner 2026-07-25.

**Milestone 7 (Release candidate audit) is complete — the final milestone.** Per the project owner's explicit framing, this audit deliberately checked things *not* exercised during implementation, plus a specific reproducibility check they requested: re-ran the backend suite and frontend build/lint/tsc fresh at HEAD (matching Milestone 6's numbers exactly, confirming evidence still applies); ran a **fresh-checkout reproducibility check** — copied the working tree into a clean directory (excluding all caches, since nothing this phase changed has been committed yet, so a `git worktree` from HEAD would have tested the wrong code), installed backend deps into a brand-new venv and frontend deps via a clean `npm install` (a pre-existing, unrelated `npm ci` lockfile-drift issue was found and correctly routed around, not fixed — it's already documented in `frontend/Dockerfile`'s own comment and confirmed via `git diff` to predate this phase entirely); built and ran the full stack in an isolated Docker Compose project (fresh volume, exactly the five expected migrations, restart-then-persistence confirmed, the Workbench "Converter" tile confirmed live in the actual containerized build, fully torn down afterward); confirmed zero new dependencies via `git diff` on all four manifest/lockfiles; confirmed repository cleanliness; and drafted `10_RELEASE_NOTES.md` following the repo's existing per-phase convention. Zero open BLOCKERs — the phase's one real finding (the Workbench dependency) was already caught and fixed in Milestone 5, not an open item here. Awaiting the project owner's formal Sign-off decision.

## Completed

- [x] Discovery pass over the shipped Converters and Ingest pipelines (backend services, routes, schemas; frontend features, pages, nav-registry) and the relevant FDK docs (`02_ROADMAP.md`, `03_ARCHITECTURE.md`, `06_TECH_STACK.md`, `07_CODING_STANDARDS.md`, `01_PRODUCT_PRINCIPLES.md`).
- [x] Four blocking scope questions raised to and resolved by the project owner (see Session Notes below).
- [x] `01_SPEC.md` filled in (v0.2.0): summary, user stories, functional requirements, relationship to existing features, explicitly out of scope.
- [x] `02_UI.md` filled in: unified `/converters` page design, nav-registry consolidation, states, responsive/accessibility carry-over.
- [x] `03_BACKEND.md` filled in: `ConverterProvider` interface/registry design, wrap-not-rewrite rationale, trade-off discussion.
- [x] `04_DATABASE.md` filled in: explicitly zero schema/migration impact.
- [x] `06_API.md` filled in: one new additive endpoint, full backward-compatibility accounting of existing endpoints.
- [x] `README.md` scope/deliverables/milestones/risks TODOs filled in.
- [x] `08_ACCEPTANCE.md` filled in: functional, non-regression, UX, and quality acceptance criteria — expanded from the project owner's explicit release-gate list (every existing conversion works, no API contract changes, browser converters never upload data, Ingest preserved, unified nav, `/ingest` redirect, no migrations, no perf regression, all existing tests pass) into independently-verifiable checklist items, each traced to a `01_SPEC.md` §3 requirement or stated as a standalone non-regression gate. Unchecked — these are pre-implementation release gates, not yet verified.

- [x] `07_TESTING.md` filled in: backend baseline/characterization tests for the previously-untested Converters/Ingest pipeline (a real coverage gap found and closed as part of this phase, not deferred), new provider/API tests, a manual verification script tracing each step to an acceptance criterion, and an explicit regression-risk section (this phase, unlike Prompt Studio, *is* regression risk by nature).
- [x] `09_IMPLEMENTATION_TASKS.md` filled in: seven-milestone breakdown (Baseline tests → Provider interface/registry → Ingest provider integration → Browser provider abstraction → Unified UI/routing → Cleanup/hardening → RC audit), each with objective, scope, files/modules, dependencies, acceptance criteria, tests to execute, rollback considerations, and an S/M/L complexity estimate, per the project owner's explicit Stage 4 requirements. `README.md`'s Milestones section updated to match.
- [x] **Implementation authorized** (2026-07-23) — `IMPLEMENT.md` updated to Authorized status; the milestone-boundary review cadence and the no-simultaneous-architecture-and-behavior rule are recorded there and in `09_IMPLEMENTATION_TASKS.md` §0.1.
- [x] **Milestone 1 (T1–T3) complete** — `backend/tests/test_converters_cron.py` (6 tests) and `backend/tests/test_ingest_pipeline.py` (21 tests), all passing against the current, unmodified pipeline. Full backend suite: 138 → 165 passed, 0 failures, 0 errors. Approved by the project owner 2026-07-25.
- [x] **Milestone 2 (T4–T5) complete** — `backend/app/services/converters/providers/__init__.py` (new), `backend/tests/test_converters_providers.py` (7 tests, fake provider only). Full backend suite: 165 → 172 passed, 0 failures, 0 errors. Approved by the project owner 2026-07-25.
- [x] **Milestone 3 (T6–T8) complete** — `backend/app/services/converters/providers/markitdown.py` (new), `schemas/converters.py`/`api/routes/converters.py` additions (`GET /providers`), 11 new tests across `test_converters_providers.py` (extended) and `test_converters_api.py` (new). Full backend suite: 172 → 183 passed, 0 failures, 0 errors. `services/ingest/` confirmed byte-for-byte unmodified.
- [x] **Milestone 4 (T9–T10) complete** — `frontend/features/converters/providers.ts` (new, ten-entry manifest). Zero existing tool files touched. Verified via browser: manifest-driven render identical to today's page for all ten tools, including live interaction (JSON Minify, Cron's real `POST /api/converters/cron/parse` call).
- [x] **Milestone 5 (T11–T14) complete** — unified `/converters` page, `/ingest` redirect (`next.config.ts`), `nav-registry.ts` consolidation. Plus an unplanned but necessary fix: `backend/app/services/workbench.py` and `frontend/features/workbench/tool-metadata.ts` updated to retire the dead `"ingest"` key and the redundant `"universal_converter"` placeholder (both folded into `"converters"`), with one pre-existing test literal corrected in `backend/tests/test_workbench.py`. Full backend suite: 183 passed, 0 regressions. `tsc`/`lint`/`build` clean.
- [x] **Milestone 6 (T15–T17) complete** — full regression sweep (183 tests, zero new migrations, zero diff in the wrapped Ingest pipeline, measured endpoint latency), shipped-app docs updated (`docs/Architecture.md`, `docs/API.md`, `docs/FolderStructure.md`, root `README.md`, `forge-docs/02_ROADMAP.md`), dead-code sweep clean. Every `08_ACCEPTANCE.md` §1–§4 criterion now checked with cited evidence.
- [x] **Milestone 7 (T18–T21) complete — the final milestone.** RC audit: acceptance criteria re-verified fresh at HEAD; a fresh-checkout reproducibility check (added at the project owner's explicit request); a Docker Compose clean-room build/up/restart/teardown; a dependency review (zero new dependencies); a repository-cleanliness check; and a drafted `10_RELEASE_NOTES.md`. Zero open BLOCKERs.

## In Progress

- [ ] Awaiting the project owner's formal Sign-off decision (per `08_ACCEPTANCE.md` §5 and `13_PHASE_LIFECYCLE.md`) — the only remaining step. This is a decision for the project owner to make, not an implementation task.

## Remaining

- [ ] None from an implementation standpoint. Post-sign-off: tag/merge to `master` (Released), then freeze this phase's specification per `13_PHASE_LIFECYCLE.md` — both are the project owner's actions to take when ready, not open work items here.

## Known Issues

- **Pre-existing, not introduced by this phase:** a `.jpg`/image file with no vision-LLM assistance configured (the default) fails to convert — pinned as a test (`test_ingest_pipeline.py::test_convert_image_without_vision_fails_with_no_text_content`), not fixed, since fixing it would be a new capability out of this phase's scope. See `10_RELEASE_NOTES.md`'s Known Issues.
- **Pre-existing, not introduced by this phase:** `npm ci` fails against this repo's current lockfile (Windows-generated optional-dependency entries don't satisfy any platform) — already documented in `frontend/Dockerfile`'s own comment; the production build already correctly uses `npm install` instead. Discovered while building Milestone 7's T20a reproducibility check; confirmed via `git diff` to predate this phase (`package.json`/`package-lock.json` both byte-for-byte unchanged).

## Architectural Decisions

- The `ConverterProvider` interface (see `03_BACKEND.md` §2) is introduced with only one provider registered this phase — an explicit trade-off flagged for owner review, not yet a ratified ADR. If the owner judges this premature, `01_SPEC.md` requirements 6–7 become no-ops and the phase ships as pure UI/nav consolidation.

## Modified Files

- Documentation: `01_SPEC.md`, `02_UI.md`, `03_BACKEND.md`, `04_DATABASE.md`, `06_API.md`, `08_ACCEPTANCE.md`, `07_TESTING.md`, `09_IMPLEMENTATION_TASKS.md`, `README.md`, `IMPLEMENT.md`, this file.
- Code (new, test-only — Milestone 1): `backend/tests/test_converters_cron.py`, `backend/tests/test_ingest_pipeline.py`. Zero production code touched.
- Code (new — Milestone 2): `backend/app/services/converters/providers/__init__.py` (the `ConverterProvider` protocol + registry — additive scaffolding, not wired to anything real yet), `backend/tests/test_converters_providers.py`.
- Code (new/modified — Milestone 3): `backend/app/services/converters/providers/markitdown.py` (new), `backend/app/schemas/converters.py` (modified — added `ProviderOut`/`ProvidersOut`), `backend/app/api/routes/converters.py` (modified — added `GET /providers`), `backend/tests/test_converters_providers.py` (extended), `backend/tests/test_converters_api.py` (new). `services/ingest/` itself: zero changes.
- Code (new — Milestone 4): `frontend/features/converters/providers.ts`. Zero existing tool files touched. A throwaway verification page (`app/(app)/m4-smoke-test/`) was created and deleted within this milestone — not part of the shipped change set.
- Code (new/modified/deleted — Milestone 5): `frontend/app/(app)/converters/page.tsx` (rewritten), `frontend/app/(app)/ingest/page.tsx` (deleted), `frontend/next.config.ts` (added `/ingest` redirect), `frontend/lib/nav-registry.ts` (consolidated). Unplanned but necessary: `backend/app/services/workbench.py`, `frontend/features/workbench/tool-metadata.ts`, `backend/tests/test_workbench.py` (retired the dead `"ingest"` key and the redundant `"universal_converter"` placeholder — see Milestone 5's completion note in `09_IMPLEMENTATION_TASKS.md` for the full rationale). A throwaway verification server pair (`backend-verify`/`frontend-verify`) and its scratch data dir were used and cleaned up.
- Documentation (Milestone 6): `docs/Architecture.md`, `docs/API.md`, `docs/FolderStructure.md`, root `README.md`, `forge-docs/02_ROADMAP.md`. No production code changed in this milestone (verification and docs only, as expected for its architecture-neither/behavior-neither classification).
- Documentation (Milestone 7): new `10_RELEASE_NOTES.md`. No production code changed — a throwaway fresh-checkout copy (`~/forge-repro-check/`) and an isolated Docker Compose project (`forge-rc-audit`) were both used for verification and fully cleaned up/torn down afterward.

## Next Milestone

None. Phase 04 is Released & Frozen. Future work against this phase is limited to genuine bug fixes, per `13_PHASE_LIFECYCLE.md` §4.

## Next Claude Prompt

```
Phase 04 (Universal Converter) is released, merged (PR #19, commit
8b5ee07), tagged v0.4.0-universal-converter, and frozen - see
forge-docs/history/2026-07-25-phase-04-final-checkpoint.md. Begin Stage 1
(Specification) for Phase 05 (Model Playground) per
14_IMPLEMENTATION_PLAYBOOK.md, following the same disciplined process used
for Phases 01-04: produce a complete spec before any code, flag ambiguities
for clarification rather than assuming, and do not proceed to Stage 2
without explicit owner approval of the spec.
```

## Session Notes

- 2026-07-20 — Phase scaffold created by the Lead Architect FDK setup. No implementation work has occurred.
- 2026-07-23 — Specification session. Discovery pass completed over the existing Converters/Ingest pipelines and relevant FDK docs (see Completed above). Four blocking scope questions were raised to the project owner and resolved:
  1. **Scope of new capability** — "Consolidation-first": no new conversion directions this phase; genuinely new directions (e.g. Markdown→DOCX, server-side JSON↔YAML↔XML) deferred to a future fast-follow. *Recommended option accepted.*
  2. **Text converter execution location** — client-side execution (JSON/YAML/XML/CSV/regex/URL/Unicode/cron/timestamp/diff) stays as-is; not moved to the backend. *Recommended option accepted.*
  3. **UI consolidation** — single unified page/nav entry replacing the two current entries (Converters, Ingest), with a redirect from `/ingest`. *Recommended option accepted.*
  4. **Persistence** — no new database tables; file-based conversion keeps Ingest's exact ephemeral/TTL-cleaned model. *Recommended option accepted.*

  `01_SPEC.md`, `02_UI.md`, `03_BACKEND.md`, `04_DATABASE.md`, `06_API.md`, and `README.md` were filled in against these four decisions. Awaiting project-owner review before Stage 4 (implementation task breakdown) and `IMPLEMENT.md` authorization.
- 2026-07-23 (same session, continued) — **Project owner authorized implementation**, unlocking Milestone 1, and set two standing rules: a project-owner review at every milestone boundary (not only at the end), and no milestone may pair an architectural change with a behavioral change unless its stated objective explicitly requires both. `IMPLEMENT.md` updated to Authorized; `09_IMPLEMENTATION_TASKS.md` §0.1 added, classifying each of the seven milestones against the new rule (Milestones 3 and 5 are the two where the rule's exception explicitly applies, per their own stated objectives).

  **Milestone 1 (Baseline characterization tests, T1–T3) executed and completed.** Checked the backend virtualenv (`.venv`, Python 3.13.5) for the libraries needed to generate real test fixtures (`markitdown`, `python-docx`, `reportlab`, `openpyxl`, `Pillow` — all present) rather than guessing. Ran a throwaway probe script first to observe actual MarkItDown conversion output for `.txt`/`.html`/`.docx`/`.xlsx`/`.pdf`/`.jpg`/`.zip` fixtures, then wrote test assertions to match the real, observed behavior — not the expected/guessed behavior — per the "characterization test" discipline this milestone exists to establish. One real finding worth recording: a `.jpg` with no caption-worthy content, converted with vision disabled (today's default), fails with `status == "error"` ("Conversion produced no text content") rather than producing any output — a genuine, pre-existing gap in image handling, now pinned as a test rather than left an undocumented surprise, and explicitly out of this phase's scope to fix (per `01_SPEC.md` §5, no new conversion capability ships in Phase 04).

  Wrote `backend/tests/test_converters_cron.py` (6 tests, closing `cron.py`'s previously-zero coverage) and `backend/tests/test_ingest_pipeline.py` (21 tests: `postprocess.clean_markdown`, `formats.KNOWN_EXTENSIONS` snapshot, `jobs.JobStore`/`Job` status/progress/TTL-cleanup, real `converter._convert_one` conversions across six format families, and `vision.pdf_needs_vision`'s default-off behavior). Confirmed the full backend suite baseline (138 passed, 0 failures) before adding anything, then re-ran after: 165 passed, 0 failures, 0 errors — the 27 new tests, zero regressions, zero pre-existing test affected. `JobStore`/`Job`-dir tests use a per-test fake settings object via `monkeypatch` rather than the module's real global singleton, so nothing here can collide with any other test module or leave scratch data behind (`tmp_path` handles cleanup automatically). Zero production code touched, satisfying Milestone 1's §0.1 classification (neither architectural nor behavioral change).

  Per the new review cadence, stopping here for the project owner's Milestone 1 review — Milestone 2 (Provider interface and registry) has not started.
- 2026-07-25 — **Project owner approved Milestone 1** and authorized proceeding to Milestone 2 (Provider interface and registry, T4–T5).

  Wrote `backend/app/services/converters/providers/__init__.py`: the `ConverterProvider` `Protocol` (`slug`, `label`, `input_extensions`, `output_format`, `submit(job)`), a module-level `_PROVIDERS` list, `register()`, `list_providers()`. `submit`'s `job` parameter is deliberately left untyped — no `Job` import from `services.ingest` — so this module has zero dependency on any concrete pipeline at this stage, per this milestone's architecture-only classification (§0.1 in `09_IMPLEMENTATION_TASKS.md`). Confirmed this directly rather than by convention: an AST parse of the new file shows its only two imports are `__future__` and `typing`.

  Wrote `backend/tests/test_converters_providers.py` (7 tests) exercised only against a throwaway `_FakeProvider` defined in the test file — empty-registry start, single/multiple registration, `list_providers()` returning a defensive copy, `runtime_checkable` protocol conformance, and confirming the registry itself never calls `submit`. An `autouse` fixture resets `providers._PROVIDERS` before every test so nothing here leaks into Milestone 3's planned extension of this same test file. Full backend suite: 165 → 172 passed, 0 failures, 0 errors.

  Per the change-type rule, Milestone 2 is classified as architecture-only (a new protocol/registry) with zero behavioral change (nothing in the running app calls `register()` with a real provider yet). Stopping here for the project owner's Milestone 2 review — Milestone 3 (Ingest provider integration) has not started.
- 2026-07-25 (same session, continued) — **Project owner approved Milestone 2**, noting the AST-based import check as "a strong extra guardrail," and authorized proceeding to Milestone 3 (Ingest provider integration, T6–T8).

  Wrote `backend/app/services/converters/providers/markitdown.py`: `MarkItDownProvider` (`slug="markitdown"`, `input_extensions=formats.KNOWN_EXTENSIONS`, `output_format="markdown"`), whose `submit()` is a pure one-line delegation to `services.ingest.converter.submit` — no transformation, no re-implementation. Added `ProviderOut`/`ProvidersOut` to `schemas/converters.py` and `GET /providers` to `api/routes/converters.py` (importing `providers.markitdown` for its registration side effect, `AuthDep`-gated identically to the existing `cron/parse` route).

  Extended `test_converters_providers.py` with 2 tests against the real `MarkItDownProvider` (declared shape; a monkeypatch-spy proof that `submit()` calls `converter.submit()` exactly once, unchanged). Wrote `test_converters_api.py` (9 tests): the new endpoint's `200`/`401`, the full `cron/parse` contract, the complete Ingest lifecycle end-to-end (upload → poll → content → download → zip-all → save-to-notes), an OpenAPI presence check for every pre-existing path plus the new one, and an explicit unauthenticated-request check.

  **Two real issues surfaced while actually running these tests (not assumed to pass):**
  1. A genuine bug in the *tests'* own design, not production code: the first draft imported `markitdown` lazily inside a test function, which ran after `test_converters_providers.py`'s Milestone-2 autouse fixture had already swapped `_PROVIDERS` for an empty per-test list — so the one-time, process-wide `register(MarkItDownProvider())` side effect fired into that throwaway list and was discarded at teardown, leaving the real registry empty and the endpoint returning zero providers. Fixed by moving the `markitdown` import to module level, so registration happens once at collection time, before any fixture can shadow it. Verified the fix holds by re-running the two files in every collection order (each alone, each first) — all green in every ordering.
  2. A real, pre-existing, Windows-only detail in the *unmodified* `converter.py`: `out_path.write_text(markdown, encoding="utf-8")` uses Python's default text-mode newline translation, which is a no-op on Linux (the real deployment target) but converts `\n` to `\r\n` on this Windows dev environment. This only surfaced because the download test reads raw HTTP response bytes rather than text (Milestone 1's tests read via `.read_text()`, which normalizes it away). The test was adjusted to normalize line endings before comparing, with a comment explaining why — the pipeline code itself is untouched, since fixing a pre-existing, platform-only quirk is out of this phase's scope.

  Confirmed `git diff --stat app/services/ingest/` is empty — the wrapped pipeline is genuinely untouched, not just believed to be. Milestone 1's baseline tests re-ran unchanged and green. Full backend suite: 172 → 183 passed, 0 failures, 0 errors.

  Per the change-type rule, Milestone 3 is classified as **both** architectural (wiring the real provider into the registry) and behavioral (the new endpoint) — exactly the pairing pre-authorized by this milestone's own stated objective, per §0.1. Stopping here for the project owner's Milestone 3 review — Milestone 4 (Browser provider abstraction) has not started.
- 2026-07-25 (same session, continued) — **Project owner approved Milestone 3**, endorsing the registration-timing fix and the newline-normalization boundary specifically, and authorized proceeding to Milestone 4 (Browser provider abstraction). Before starting, confirmed with the owner that "T9–T11" in their instruction should stay scoped to the approved plan's T9–T10 (manifest + verification, architecture-only) — T11 (wiring a live page to the manifest) stays in Milestone 5, where §0.1 classifies pairing an architectural and behavioral change as pre-justified.

  Wrote `frontend/features/converters/providers.ts`: `CONVERTER_TOOL_PROVIDERS`, a `{slug, label, description, Component}` array referencing all nine existing tool files unmodified. Caught and corrected one imprecision in the milestone's own plan wording while building it: `url-unicode-tool.tsx` exports *two* components (`UrlEncodeTool` and `UnicodeInspector`), both already separate cards on today's page — so the manifest has ten entries from nine files, not nine, matching today's page exactly rather than the plan's shorthand count.

  Verified, not assumed: `git status --short frontend/features/converters/` showed only the new file. `tsc --noEmit`, `npm run lint`, `npm run build` all clean. Built a throwaway page rendering the manifest, started the project's isolated `backend-verify`/`frontend-verify` dev servers (per `.claude/launch.json`, not the user's own instance), completed first-run setup, and compared it side by side against the real `/converters` page — identical content and order for all ten tools. Went past a static comparison: clicked JSON's Minify button through the manifest-rendered component (correct output) and clicked Cron's Parse button, confirming a real `POST /api/converters/cron/parse` → `200 OK` fired and rendered correctly through a component reached only via the manifest — proving the indirection doesn't break the one tool with real backend interaction. Deleted the throwaway page and the verification servers' scratch dev-data afterward; re-confirmed `tsc`/`lint` clean with it gone.

  Per the change-type rule, Milestone 4 is classified as architecture-only (the manifest) with zero behavioral change (nothing in the live app consumes it — the verification page was deleted, not shipped). Stopping here for the project owner's Milestone 4 review — Milestone 5 (Unified Converter UI and routing) has not started.
- 2026-07-25 (same session, continued) — **Project owner approved Milestone 4**, specifically noting the manifest accurately modeling the live page (including the two components from `url-unicode-tool.tsx`) and the live-interaction proof as evidence of a faithful, not just structurally valid, abstraction. Authorized proceeding to Milestone 5 (Unified Converter UI and routing). A numbering mismatch in the instruction ("T12–T15") was flagged and clarified before starting: proceeded with the approved plan's T11–T14, since T15 belongs to Milestone 6.

  Rewrote `converters/page.tsx` to render "Text & Data" (Milestone 4's manifest) and "Documents" (the unmodified Ingest components) as two sections. Retired `/ingest`: deleted its `page.tsx`, added a `next.config.ts` redirect (`permanent: false`, following the `/vault` → `/secrets` alias's exact precedent). Consolidated `nav-registry.ts` to one "Converter" entry.

  **A real, unplanned cross-feature dependency was found and fixed, not glossed over.** Workbench (Phase 01, frozen) has its own documented "manually synced" nav-to-pinned-tool mapping. It carried `"ingest"` as the *first* item in `DEFAULT_PINNED_TOOLS` — meaning every fresh install pins "Ingest" by default — plus a forward-looking `"universal_converter": {"available": False}` placeholder explicitly anticipating this phase (mirroring the `prompt_studio` placeholder Phase 03 flipped on shipping). Removing `/ingest` from `nav-registry.ts` without addressing this would have silently dropped the default-pinned tile (the lookup fails soft to `null`, not a crash — a quiet regression, not a loud one). Fixed by retiring both the dead `"ingest"` key and the redundant `"universal_converter"` placeholder in favor of the single `"converters"` key, across `backend/app/services/workbench.py` and `frontend/features/workbench/tool-metadata.ts`; one pre-existing test (`test_workbench.py::test_reset_layout_restores_the_default_layout`) hardcoded the pinned-tools count as a literal `== 6` in addition to the symbolic comparison, corrected to `== 5` with an explanatory comment. Treated as in-scope maintenance of an already-documented seam directly caused by this phase's own nav change, not new Workbench feature work — the same judgment Phase 03 recorded for its own placeholder flip.

  Verified, not assumed: full backend suite (183 passed, 0 regressions — one test literal corrected); `tsc`/`lint`/`build` clean (after clearing a stale `.next` cache reference to the deleted route). Live-verified against the project's isolated `backend-verify`/`frontend-verify` servers: single nav/palette "Converter" entry; **the Workbench pinned-tools panel correctly showing a "Converter" tile**, direct positive confirmation the fix works; the `/ingest` redirect confirmed via `window.location` and a raw `curl` check (`307` → `/converters`); zero network requests from a client-side tool click; no horizontal overflow at 375px; dark mode applies; 46 focusable elements found (keyboard spot-check). Cleaned up the throwaway servers and scratch dev-data afterward.

  Per the change-type rule, Milestone 5 is classified as both architectural and behavioral — the pairing pre-authorized by its own stated objective — with the Workbench fix classified the same way, as a necessary consequence of that objective, not a separate unauthorized change. Stopping here for the project owner's Milestone 5 review — Milestone 6 (Cleanup, documentation, and regression hardening) has not started.
- 2026-07-25 (same session, continued) — **Project owner approved Milestone 5**, specifically praising the unified page as the single entry point, the redirect matching existing precedent, the Workbench fix keeping the default pinned tile aligned, the pre-existing test-literal correction as strengthening rather than masking, and the behavioral (not just structural) nature of the verification. Authorized proceeding to Milestone 6 (Cleanup, documentation, and regression hardening). A numbering mismatch ("T16–T18") was again flagged and clarified before starting: proceeded with the approved plan's T15–T17, since T18 belongs to Milestone 7.

  **T15 (full regression sweep):** Re-ran the full backend suite fresh (183 passed), confirmed `alembic/versions/` unchanged at 5 files, confirmed `services/ingest/`/`cron.py` still zero-diff, and measured `list_providers()` directly (1000 calls, avg 0.21μs) instead of asserting its performance from code reading. Ran a complete, continuous manual golden path in one session for the first time (previous milestones each verified their own slice) against the isolated `backend-verify`/`frontend-verify` servers: unified page, single nav/palette entry, a genuine file drop through the dropzone's file input (a synthetic `DataTransfer` + `change` event, not just the existing API-level test), the job polling through to completion, mobile width and dark mode checked together, and a keyboard-reachability spot check.

  Investigated rather than dismissed a moment of real confusion: the live upload appeared stuck at "0 of 1 converted" while a direct API check confirmed the job was already `"done"`. Traced to this automation environment's background-tab timer throttling delaying the UI's 800ms poll — waiting a real few seconds (not a rapid recheck) converged it correctly, confirmed by repeating with a second upload. Separately, a too-broad button selector accidentally clicked "Lock" instead of the job list's preview icon — caught immediately, the app handled it correctly (returned to the unlock screen, no crash), and wasn't a defect.

  **T16 (documentation):** Updated `docs/Architecture.md` (renamed "Ingest" to "Converter," describing the unification and the provider registry), `docs/API.md`, `docs/FolderStructure.md`, root `README.md` (collapsed two feature bullets into one), and `forge-docs/02_ROADMAP.md` (Phase 04's status row and the document's own header were stale at "Not started" — updated to reflect Milestones 1–6 complete).

  **T17 (dead-code check):** Repo-wide grep for stale `/ingest` links, "Ingest" nav references, and leftover verification artifacts (`m4-smoke-test`, `m6-sweep`) — all clean. Confirmed the two remaining `"ingest"`/`"universal_converter"` matches in `workbench.py` are Milestone 5's own explanatory comments, not live code.

  Updated `08_ACCEPTANCE.md`: every criterion in §1–§4 is now checked against a specific cited test, live action, or code inspection — not left as unverified release gates. One criterion (`docker compose build && up`) deliberately left unchecked, explicitly deferred to Milestone 7's T20 per the milestone plan's own division of labor, not overlooked.

  Per the change-type rule, Milestone 6 is classified as neither architectural nor behavioral — verification and documentation only, as expected. Stopping here for the project owner's Milestone 6 review — Milestone 7 (Release candidate audit), the final milestone, has not started.
- 2026-07-25 (same session, continued) — **Project owner approved Milestone 6** and authorized proceeding to Milestone 7, explicitly framing it as an audit of things intentionally *not* exercised during implementation (Docker build/up, a clean production build, lint/typecheck/build from scratch, a final dependency review, repository cleanliness, release-notes consistency, and confirming acceptance evidence still matches HEAD) rather than another round of feature verification — and added one new check: a fresh-checkout reproducibility test (clone/checkout into a clean directory, install from lockfiles only, confirm the documented commands work without relying on cached build artifacts). Recorded this amendment in `09_IMPLEMENTATION_TASKS.md` (renumbering the original Docker task T20b and adding T20a/c/d/e) before executing. A numbering mismatch ("T18–T21" stated as the range but phrased inclusive of items now split across five sub-tasks) was reconciled the same way as prior mismatches — proceeded with the plan as amended.

  **T18:** Re-ran the backend suite (183 passed) and frontend build/lint/tsc fresh at current HEAD — identical to Milestone 6's numbers, confirming the evidence in `08_ACCEPTANCE.md` still holds.

  **T19:** Zero new findings from this milestone's own checks; per `12_BUG_CLASSIFICATION.md` §2, zero BLOCKERs. The phase's one real finding (the Workbench dependency) was already fixed in Milestone 5.

  **T20a (added):** Copied the current working tree into a clean directory via `robocopy /XD` (excluding `node_modules`/`.venv`/`.venv312`/`.next`/`__pycache__`/`.dev-data*`/`.git`) rather than a `git worktree` — deliberately, since nothing this phase has done is committed yet, and a worktree from HEAD would have silently tested the pre-phase code instead of what's actually being delivered. Backend: brand-new venv, `pip install -r requirements.txt -r requirements-dev.txt`, full suite green (183 passed) with zero reliance on the dev `.venv`. Frontend: `npm ci` failed — investigated rather than dismissed, traced to a genuinely pre-existing, already-documented condition (`frontend/Dockerfile`'s own comment explains the lockfile has Windows-only optional-dependency entries that don't satisfy `npm ci` on any platform), confirmed via `git diff` to predate this phase entirely (`package.json`/`package-lock.json` both untouched). Redid the check with `npm install` (the Dockerfile's actual documented command) — clean install, `tsc`/`lint`/`build` all passed from the fresh copy. Cleaned up afterward.

  **T20b:** Built and ran the full stack in an isolated Compose project (`forge-rc-audit`, port 8587, brand-new volume) — confirmed via logs that only the five pre-existing migrations applied. Live-verified against the real containerized image: setup/unlock, the unified page, the redirect (`307` via `curl`), and — the most important check — the Workbench pinned-tools panel correctly showing "Converter," proving Milestone 5's fix holds in production, not just the dev server. Restarted the stack: migration re-run was a no-op, session and pinned-tools state both persisted. Fully torn down (`down -v`) afterward.

  **T20c:** `git diff` on all four dependency manifests (`backend/requirements.txt`, `backend/requirements-dev.txt`, `frontend/package.json`, `frontend/package-lock.json`) against the pre-phase baseline — all empty. Zero new dependencies.

  **T20d:** `git status --short` shows exactly this phase's documented changes, nothing stray; confirmed `__pycache__`/`.pyc` files on disk are properly gitignored, not tracked.

  **T20e:** Found the repo's existing `10_RELEASE_NOTES.md` per-phase convention (templated, precedented by Phase 02) and drafted one for this phase, disclosing both pre-existing conditions found during the audit (the `.jpg`-without-vision gap, the `npm ci` lockfile drift) under Known Issues rather than omitting them.

  **T21:** This document, `README.md`, and `IMPLEMENT.md` updated to reflect implementation-complete status. Per the change-type rule, Milestone 7 introduced neither an architectural nor a behavioral change, as expected for an audit-only milestone.

  Zero open BLOCKERs. Phase 04's implementation is complete. The only remaining step is the project owner's formal Sign-off decision.
- 2026-07-25 (same session, continued) — **Project owner recorded formal Sign-off: APPROVED.** Release Candidate accepted, zero blocking issues, known issues limited to those already documented in `10_RELEASE_NOTES.md`. Directed proceeding with the project's normal release process and explicitly noted that further code changes are inappropriate from here except to address a newly discovered BLOCKER — any enhancement begins as a new phase.

  Executed the release process, confirming the established workflow first (three prior phases each used a feature branch → PR → squash-merge → tag → separate post-release freeze/archive commit, evidenced by `git log`, `git branch -a`, and the existing `v0.1.0`–`v0.3.0` tags) rather than assuming a different mechanism:
  - Created branch `Phase04/Universal-Converter`, committed all Phase 04 changes (33 files), pushed to origin.
  - Opened [PR #19](https://github.com/DiegoDoug/forge/pull/19), squash-merged to `master` (commit `8b5ee07`), deleted the feature branch.
  - Tagged `v0.4.0-universal-converter` on the merge commit, pushed the tag.
  - Archived end-of-phase artifacts per `13_PHASE_LIFECYCLE.md` §5 and Phase 01/03's exact precedent: `git mv`'d `10_RELEASE_NOTES.md` to `forge-docs/history/Phase-04/`, fixed its internal cross-references for the new location, finalized its status from Draft to Released & Frozen. Updated this document's and `README.md`'s status headers to 🔒 RELEASED & FROZEN, referencing PR #19 and commit `8b5ee07` (matching Phase 03's exact wording pattern). No version bump to `backend/app/core/version.py` or `frontend/package.json` (both still `0.1.0`) — following Phase 02/03's own precedent of not bumping these at release.
  - Updated `forge-docs/02_ROADMAP.md`: header Status/Version/Last-Updated, Phase 04's table row to `✓ Complete — 🔒 released & frozen as v0.4.0-universal-converter`, and §4's sequencing rationale.
  - Wrote `forge-docs/history/2026-07-25-phase-04-final-checkpoint.md`, mirroring Phase 03's final-checkpoint template exactly.

  This phase's directory now accepts bug fixes only, per `13_PHASE_LIFECYCLE.md` §4. Phase 04 (Universal Converter) is released.
- 2026-07-23 (same session, continued) — `08_ACCEPTANCE.md` filled in at the project owner's request, ahead of the task breakdown. The owner supplied nine release-gate categories (every existing conversion works, no API contract changes, browser converters never upload data, Ingest preserved, unified nav replaces old entries, `/ingest` redirects, no DB migrations, no performance regression, all existing tests pass); each was expanded into one or more independently-verifiable criteria (specific commands, diffs, or observations) rather than left as a subjective review point, per the owner's explicit instruction that these become "objective release gates rather than subjective review points." All criteria are unchecked pending Stage 5 RC verification.
- 2026-07-23 (same session, continued) — `07_TESTING.md` filled in. The owner's approval-status message confirmed the design phase would be considered effectively complete, contingent on this document, with approval to proceed into Stage 4 (Milestone Breakdown) once it's finished and linked to the acceptance criteria. A real gap was surfaced and addressed rather than glossed over: `backend/tests/` has zero existing coverage for Converters or Ingest today, which would have made "no regression" claims unfalsifiable — this phase's test plan now includes baseline/characterization tests against the pipeline's pre-wrapper behavior, so the wrap-not-rewrite design in `03_BACKEND.md` has something concrete to be checked against. Every test in the document is traced to the specific `08_ACCEPTANCE.md` criterion it verifies. Design phase (Discovery → Specification → UI → Backend Architecture → Database → API → Acceptance Criteria → Testing Plan) is now complete per the owner's checklist.
- 2026-07-23 (same session, continued) — `09_IMPLEMENTATION_TASKS.md` (Stage 4, Milestone Breakdown) filled in. The owner authorized proceeding to Stage 4 and specified: milestones must be independently reviewable, independently testable, reversible if necessary, and small enough to isolate a regression quickly; each milestone must include objective, scope, files/modules, dependencies, acceptance criteria, tests to execute, rollback considerations, and a complexity estimate; and specified the seven-milestone sequence (Baseline tests → Provider interface/registry → Ingest provider integration → Browser provider abstraction → Unified UI/routing → Cleanup/hardening → RC audit). All seven milestones were drafted against that sequence and those requirements, with 21 individual tasks (T1–T21) traced back to specific `01_SPEC.md`/`03_BACKEND.md`/`08_ACCEPTANCE.md` items. `README.md`'s Milestones section was updated to match (the earlier 3-milestone sketch is superseded). Per the owner's explicit instruction, implementation stays locked until this milestone breakdown is reviewed and approved — Milestone 1 has not started.

## Cross-references

- [README.md](README.md)
- [09_IMPLEMENTATION_TASKS.md](09_IMPLEMENTATION_TASKS.md)
- [../../10_CHECKPOINT_PROTOCOL.md](../../10_CHECKPOINT_PROTOCOL.md)
- [../../history/README.md](../../history/README.md)
