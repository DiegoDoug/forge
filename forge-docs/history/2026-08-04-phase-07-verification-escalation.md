# FDK Verification Escalation — Phase 07 — 2026-08-04

> **Phase:** [Phase-07-Knowledge-Hub](../implementation/Phase-07-Knowledge-Hub/README.md)
> **Generated:** 2026-08-04

---

## FDK Verification Escalation

**Cycles run:** 3
**Final status:** FAIL

### Escalation conditions
- **retry_limit_exceeded**: 3 repair cycles ran with no PASS
- **architecture_spec_conflict**: the same Architecture finding recurred unchanged across every repair cycle: forge-docs/history/2026-08-04-phase-07-verification-escalation.md matches declared out-of-scope path 'forge-docs/history/'; forge-docs/history/2026-08-04-phase-07-verification-report.md matches declared out-of-scope path 'forge-docs/history/'

### Latest verification report
## FDK Verification Report — Phase Knowledge Hub (Phase 07)

| Verifier | Result |
|---|---|
| Architecture | FAIL |
| Build | PASS |
| Code Quality | PASS |
| Documentation | FAIL |
| Regression | FAIL |
| Specification | FAIL |
| Test | FAIL |
| UX | N/A |

**Overall status:** FAIL
**Recommendation:** REPAIR

### Warnings
- deeper architectural checks (circular imports, duplicated services, layering) are out of this generic verifier's scope — see architecture.py's module docstring
- forge-docs/03_ARCHITECTURE.md:5: stray TODO/FIXME marker
- forge-docs/03_ARCHITECTURE.md:79: stray TODO/FIXME marker
- forge-docs/03_ARCHITECTURE.md:81: stray TODO/FIXME marker
- forge-docs/03_ARCHITECTURE.md:84: stray TODO/FIXME marker
- forge-docs/03_ARCHITECTURE.md:86: stray TODO/FIXME marker
- forge-docs/03_ARCHITECTURE.md:87: stray TODO/FIXME marker
- forge-docs/decisions/README.md:5: stray TODO/FIXME marker
- forge-docs/decisions/README.md:26: stray TODO/FIXME marker
- forge-docs/decisions/README.md:50: stray TODO/FIXME marker
- forge-docs/decisions/README.md:52: stray TODO/FIXME marker
- forge-docs/decisions/README.md:53: stray TODO/FIXME marker
- forge-docs/implementation/Phase-07-Knowledge-Hub/07_TESTING.md:27: stray TODO/FIXME marker
- forge-docs/implementation/Phase-07-Knowledge-Hub/README.md:54: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:35: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:36: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:37: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:38: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:39: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:40: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:41: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:42: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:43: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:44: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:45: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:46: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:47: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-escalation.md:48: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:28: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:29: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:30: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:31: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:32: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:33: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:34: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:35: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:36: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:37: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:38: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:39: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:40: stray TODO/FIXME marker
- forge-docs/history/2026-08-04-phase-07-verification-report.md:41: stray TODO/FIXME marker
- forge-docs/implementation/Phase-07-Knowledge-Hub/VERIFICATION_CONTRACT.md:168: stray TODO/FIXME marker
- UX criteria are structurally unverifiable in this environment (no browser automation) — track manually as QA tickets per forge-docs/12_BUG_CLASSIFICATION.md §4

### Failed verifiers
- Architecture: forge-docs/history/2026-08-04-phase-07-verification-escalation.md matches declared out-of-scope path 'forge-docs/history/'; forge-docs/history/2026-08-04-phase-07-verification-report.md matches declared out-of-scope path 'forge-docs/history/'
- Documentation: required documentation update not found among changed files: `forge-docs/implementation/Phase-07-Knowledge-Hub/10_RELEASE_NOTES.md` authored at RC and finalized at sign-off.; required documentation update not found among changed files: `docs/Architecture.md` updated if the consolidation changes a documented boundary.; required documentation update not found among changed files: `forge-docs/implementation/README.md` phase status row updated.; required documentation update not found among changed files: `forge-docs/02_ROADMAP.md` updated to reflect Phase 07's state.
- Regression: regression target not confirmed: Notes — create/edit/delete, pin, archive, and FTS5-indexed search still work.; regression target not confirmed: Documents — rich text editing, history sidebar, and multi-format export still work.; regression target not confirmed: Ingest — the ingest pipeline, its TTL cleanup behavior, and the existing "save as Note" handoff still work.; regression target not confirmed: Search — `/search`, `GET /api/search`, and the ⌘K palette return the same response shape and content as before this phase, including existing Secrets coverage (`01_SPEC.md` FR21).; regression target not confirmed: Workbench — panel layout, pin picker, and drag/keyboard reorder still work.; regression target not confirmed: Projects (Phase 06) — project grouping still works and is unaffected by knowledge re-grouping.; regression target not confirmed: App shell — sidebar navigation and command palette entries for all existing features still resolve.; regression target not confirmed: Activity logging — existing activity events continue to be recorded.; regression target not confirmed: Existing Alembic migration chain still applies cleanly from an empty database.
- Specification: required task not marked complete: T1 — Models: `models/knowledge.py` (`NoteTagLink`, `DocumentTagLink`, `KnowledgeLink`), registered; no existing model modified.; required task not marked complete: T2 — Migration: `alembic/versions/0009_knowledge_hub.py`, revising `0008_projects`; additive only.; required task not marked complete: T3 — Schemas: `schemas/knowledge.py` per `06_API.md` §2, including the exactly-one-of `tag_id`/`name` validator.; required task not marked complete: T4 — Service: query. `list_knowledge` / `list_knowledge_tags` in `services/knowledge/service.py`, delegating text search to the existing `notes_service.search_notes` / `documents_service.search_documents` (no new FTS SQL), AND-combining tag filters, enforcing the fixed 100-item cap with real `total`/`truncated`, no `limit` parameter.; required task not marked complete: T5 — Service: tags. `add_tag` / `remove_tag`, reuse-by-name, never deletes a `Tag` row on unassign.; required task not marked complete: T6 — Service: links. `create_link` / `list_links` / `delete_link` / `purge_links_for`, pair normalization, self-link rejection, bidirectional duplicate detection; resolves the delete-hook direction question from `03_BACKEND.md` §2.2 explicitly.; required task not marked complete: T7 — Routes: `api/routes/knowledge.py`, the seven endpoints from `06_API.md` §1, wired into `api/router.py`, behind `AuthDep`.; required task not marked complete: T8 — Backend tests: `test_knowledge_service.py`, `test_knowledge_api.py`, migration up/down/up test, and non-regression extensions to existing Notes/Documents/Secrets/Search test coverage.; required task not marked complete: T9 — Frontend data layer: `features/knowledge/api.ts` plus the three TanStack Query hooks and invalidation rules from `05_COMPONENTS.md` §4.; required task not marked complete: T10 — Frontend Hub page: `app/(app)/knowledge/page.tsx` and the filter bar / result list / result row components; all five states, four-parameter URL sync (no page/offset), the fixed truncation notice, responsive layout.; required task not marked complete: T11 — Frontend tag & link controls: the tag picker and link panel/picker, mounted into the Documents sidebar and the Notes card surface; resolves the sticky-note layout question and the `select.tsx` workaround.; required task not marked complete: T12 — Navigation: one new `NavItem` in `lib/nav-registry.ts`; command-palette pickup is automatic.; required task not marked complete: T13 — Accessibility pass: keyboard operation, roles/names, the `aria-live` result region, focus indicators in both themes.; required task not marked complete: T14 — Regression pass: full backend suite, `tools/tests`, lint, typecheck, production build, `docker compose build`; confirms no new dependency and no outbound call/credential material introduced.; required task not marked complete: T15 — Manual browser QA: the full `07_TESTING.md` §3 script, including the Ingest→Note→Hub round trip and the `/search`/⌘K non-regression check.; required task not marked complete: T16 — Documentation & final state: `docs/API.md`, `docs/Database.md`, `docs/Architecture.md` (if applicable), `02_ROADMAP.md`, `forge-docs/implementation/README.md`, this phase's `CURRENT_STATE.md`/`README.md`; confirms frozen phases and `tools/fdk_verification/` untouched and the Definition of Done checklist satisfied.; acceptance criterion not marked complete: AC1 (FR1). `/knowledge` lists both Notes and Documents in one list. With N notes and M documents present, the Hub shows N+M items, each carrying a type badge that matches its source, a title (or "Untitled"), an excerpt, its tags, its project, and a last-updated time.; acceptance criterion not marked complete: AC2 (FR2). A query matching only a note's content returns that note; a query matching only a document's content returns that document; a query matching both returns both. A query matching neither returns zero items and the filtered-empty state.; acceptance criterion not marked complete: AC3 (FR2, §6.4). No new FTS index or content table exists. Verified by inspecting migration `0009_knowledge_hub.py` for any `fts` DDL (there must be none) and by confirming `services/knowledge/service.py` calls `notes_service.search_notes` / `documents_service.search_documents` rather than issuing its own `MATCH` SQL.; acceptance criterion not marked complete: AC4 (FR3). `type=note` returns only notes; `type=document` returns only documents; `type=all` returns both.; acceptance criterion not marked complete: AC5 (FR4, §6.8). Filtering by one tag returns exactly the items carrying it. Filtering by two tags returns only items carrying both (AND) — a fixture item carrying just one of the two selected tags is excluded. No request or UI control produces OR results; this is the only mode.; acceptance criterion not marked complete: AC6 (FR5). Filtering by a project returns only items whose `project_id` matches; unscoped items are excluded. No new scoping column was added (verified against `04_DATABASE.md` §1 — three new tables, no `ALTER TABLE`).; acceptance criterion not marked complete: AC7 (FR6). A query, a type, a tag set, and a project applied simultaneously return the intersection. With no query, results are ordered by `updated_at` descending.; acceptance criterion not marked complete: AC8 (FR7). Activating a note result navigates to `/notes`; activating a document result navigates to `/documents`. The Hub renders no note or document editor of its own. Per-item deep linking is best-effort only — neither feature has a per-item route today, and adding one is out of scope; landing on the correct page is what this criterion requires.; acceptance criterion not marked complete: AC8a (FR8a, §6.6/§6.7). `GET /api/knowledge` returns at most 100 items, ordered by `updated_at` descending before the cap is applied. With 105 matching items, the response contains exactly 100 items, `total: 105`, `truncated: true`, and the 100 returned are the most recently updated (the 5 oldest matches are excluded, not an arbitrary 5). `GET /api/knowledge` accepts no `page`, `offset`, `cursor`, or `limit` parameter — the cap is a fixed server constant, not caller-configurable, and no other Phase 07 endpoint accepts one either.; acceptance criterion not marked complete: AC9 (FR8). Four distinct states are observable: loading skeleton; "no knowledge at all" empty state; "no items match these filters" empty state with a clear-filters action; populated list. The two empty states are visibly different.; acceptance criterion not marked complete: AC10 (FR9–FR10). A tag can be assigned to a Note and to a Document from their own editing surfaces. Assigning a brand-new name creates one `tags` row; assigning an existing name reuses that row — after both operations the `tags` table contains exactly one row for that name.; acceptance criterion not marked complete: AC11 (FR11). Removing a tag from an item removes only the assignment; the `Tag` row still exists and remains assigned to any other item that had it.; acceptance criterion not marked complete: AC12 (FR12). `GET /api/knowledge/tags` returns only tags assigned to at least one Note or Document, each with a count equal to the number of matching items. A tag assigned only to a Secret does not appear.; acceptance criterion not marked complete: AC13 (FR13). After assigning knowledge tags, every Secrets endpoint returns identical responses to before, and `secret_tag_links` is unchanged. Verified by the existing Secrets test suite passing unmodified plus the explicit FR13 test.; acceptance criterion not marked complete: AC14 (FR14). Links can be created note→note, note→document, and document→document. All three combinations persist and are retrievable.; acceptance criterion not marked complete: AC15 (FR15). A link created from item A to item B appears in both A's and B's linked-items lists, with the `other` field correctly identifying the opposite item from each side.; acceptance criterion not marked complete: AC16 (FR16). Deleting a link from either side removes it from both sides. Both items still exist and are unchanged.; acceptance criterion not marked complete: AC17 (FR17). Deleting a Note or Document removes every `knowledge_links` row referencing it from either the source or target side. A direct database query for links referencing the deleted id returns zero rows.; acceptance criterion not marked complete: AC18 (FR18). Linking an item to itself returns `400 knowledge_link_self`. Creating a link that already exists — in either direction — returns `409 knowledge_link_duplicate`. Neither is silently ignored.; acceptance criterion not marked complete: AC19 (FR19). Knowledge Hub appears in the sidebar and is reachable from the ⌘K command palette.; acceptance criterion not marked complete: AC20 (FR20). Converting a file via Ingest and saving it as a Note produces an item that appears in the Hub, is taggable, and is linkable. Ingest gained no database table and no new endpoint.; acceptance criterion not marked complete: AC21 (FR21). `GET /api/search` returns the same response shape and content as before this phase, `/search` behaves identically, and the ⌘K palette still returns Secrets results. Verified by the explicit response-shape test plus the existing search tests passing unmodified. ADR-0007 is unmodified.; acceptance criterion not marked complete: AC22. Every screen state in `02_UI.md` §3 is present and reachable: Hub loading, both empty states, error-with-retry, populated; tag control loading/empty/populated; linked-items empty/populated.; acceptance criterion not marked complete: AC23. Self-link and duplicate-link errors render inline next to the control, not as a transient toast that disappears before it can be read.; acceptance criterion not marked complete: AC24. Whenever the API returns `truncated: true`, the Hub shows "Showing the first 100 of N — narrow your filters" (N is the real `total`). When `truncated: false`, no truncation notice appears at all. No result set is silently cut, and no "load more" / "next page" / page-number control exists anywhere on the Hub — this phase has no pagination.; acceptance criterion not marked complete: AC25. Active filters (`q`, `type`, `tag_id`, `project_id`) are encoded in the URL; reloading a filtered Hub URL restores exactly that filtered view. No page/offset parameter is ever part of the URL, because none exists.; acceptance criterion not marked complete: AC26. Every new interactive element — query input, type selector, tag multi-select, project selector, result rows, tag chips and their remove buttons, link picker, link remove — is reachable and operable by keyboard alone, with a visible focus indicator.; acceptance criterion not marked complete: AC27. The result count / truncation notice is inside an `aria-live="polite"` region and announces when filtering changes the result set. Tag chip remove buttons have accessible names of the form "Remove tag <name>".; acceptance criterion not marked complete: AC28. All new surfaces render correctly in light and dark theme and remain usable at mobile viewport widths, with the result list stacking rather than scrolling horizontally.; acceptance criterion not marked complete: AC29. All tests in `07_TESTING.md` pass — the full backend suite (`backend/.venv/Scripts/python.exe -m pytest backend/tests`) with the new `test_knowledge_service.py` and `test_knowledge_api.py`, and the migration up/down/up test.; acceptance criterion not marked complete: AC30. `npm --prefix frontend run lint`, `frontend/node_modules/.bin/tsc --noEmit`, `npm --prefix frontend run build`, and `docker compose build` all exit 0.; acceptance criterion not marked complete: AC31. No architectural invariant in `03_ARCHITECTURE.md` §2 is violated: routers stay thin, no cross-feature import between `notes` and `documents` was introduced, models remain the schema source of truth, and the schema change ships as explicit migration `0009_knowledge_hub.py`.; acceptance criterion not marked complete: AC32. No new external dependency was added — `frontend/package.json` and `backend/requirements.txt` are unchanged, and `06_TECH_STACK.md` needs no new entry.; acceptance criterion not marked complete: AC33. The phase adds zero outbound network calls and stores no credential material. Verified by code review of `services/knowledge/` and a grep for `httpx` / `requests` / `api_key` / `encrypted_` in the new module returning nothing.; acceptance criterion not marked complete: AC34. `08_DEFINITION_OF_DONE.md`'s feature-level checklist is satisfied.; acceptance criterion not marked complete: AC35. The FDK verification tooling is untouched and still green: `backend/.venv/Scripts/python.exe -m pytest tools/tests` passes, and `git diff` shows no change under `tools/fdk_verification/` or `tools/tests/`.; acceptance criterion not marked complete: AC36. Frozen phases are unmodified: `git diff` shows no change under `forge-docs/implementation/Phase-05-Model-Playground/` or `Phase-06-Projects/`, and no change to `forge-docs/decisions/0007-search-dedicated-page.md`.; acceptance criterion not marked complete: AC37. `docs/API.md` documents all seven new endpoints from `06_API.md` §1.; acceptance criterion not marked complete: AC38. `docs/Database.md` documents the three new tables, including the polymorphic-link integrity trade-off from `04_DATABASE.md` §2.; acceptance criterion not marked complete: AC39. `CURRENT_STATE.md` reflects reality with no stale "In Progress" items, and `README.md`'s Definition of Complete is satisfied.; acceptance criterion not marked complete: AC40. `02_ROADMAP.md` and `forge-docs/implementation/README.md` show Phase 07's correct status.
- Test: Test backend ('backend/.venv/Scripts/python.exe -m pytest backend/tests -q') exited 1: 'backend' is not recognized as an internal or external command,
operable program or batch file.
; Test FDK tooling regression ('backend/.venv/Scripts/python.exe -m pytest tools/tests -q') exited 1: 'backend' is not recognized as an internal or external command,
operable program or batch file.


### Recommended repairs
1. Architecture: forge-docs/history/2026-08-04-phase-07-verification-escalation.md matches declared out-of-scope path 'forge-docs/history/'
2. Architecture: forge-docs/history/2026-08-04-phase-07-verification-report.md matches declared out-of-scope path 'forge-docs/history/'
3. Documentation: required documentation update not found among changed files: `forge-docs/implementation/Phase-07-Knowledge-Hub/10_RELEASE_NOTES.md` authored at RC and finalized at sign-off.
4. Documentation: required documentation update not found among changed files: `docs/Architecture.md` updated if the consolidation changes a documented boundary.
5. Documentation: required documentation update not found among changed files: `forge-docs/implementation/README.md` phase status row updated.
6. Documentation: required documentation update not found among changed files: `forge-docs/02_ROADMAP.md` updated to reflect Phase 07's state.
7. Regression: regression target not confirmed: Notes — create/edit/delete, pin, archive, and FTS5-indexed search still work.
8. Regression: regression target not confirmed: Documents — rich text editing, history sidebar, and multi-format export still work.
9. Regression: regression target not confirmed: Ingest — the ingest pipeline, its TTL cleanup behavior, and the existing "save as Note" handoff still work.
10. Regression: regression target not confirmed: Search — `/search`, `GET /api/search`, and the ⌘K palette return the same response shape and content as before this phase, including existing Secrets coverage (`01_SPEC.md` FR21).
11. Regression: regression target not confirmed: Workbench — panel layout, pin picker, and drag/keyboard reorder still work.
12. Regression: regression target not confirmed: Projects (Phase 06) — project grouping still works and is unaffected by knowledge re-grouping.
13. Regression: regression target not confirmed: App shell — sidebar navigation and command palette entries for all existing features still resolve.
14. Regression: regression target not confirmed: Activity logging — existing activity events continue to be recorded.
15. Regression: regression target not confirmed: Existing Alembic migration chain still applies cleanly from an empty database.
16. Specification: required task not marked complete: T1 — Models: `models/knowledge.py` (`NoteTagLink`, `DocumentTagLink`, `KnowledgeLink`), registered; no existing model modified.
17. Specification: required task not marked complete: T2 — Migration: `alembic/versions/0009_knowledge_hub.py`, revising `0008_projects`; additive only.
18. Specification: required task not marked complete: T3 — Schemas: `schemas/knowledge.py` per `06_API.md` §2, including the exactly-one-of `tag_id`/`name` validator.
19. Specification: required task not marked complete: T4 — Service: query. `list_knowledge` / `list_knowledge_tags` in `services/knowledge/service.py`, delegating text search to the existing `notes_service.search_notes` / `documents_service.search_documents` (no new FTS SQL), AND-combining tag filters, enforcing the fixed 100-item cap with real `total`/`truncated`, no `limit` parameter.
20. Specification: required task not marked complete: T5 — Service: tags. `add_tag` / `remove_tag`, reuse-by-name, never deletes a `Tag` row on unassign.
21. Specification: required task not marked complete: T6 — Service: links. `create_link` / `list_links` / `delete_link` / `purge_links_for`, pair normalization, self-link rejection, bidirectional duplicate detection; resolves the delete-hook direction question from `03_BACKEND.md` §2.2 explicitly.
22. Specification: required task not marked complete: T7 — Routes: `api/routes/knowledge.py`, the seven endpoints from `06_API.md` §1, wired into `api/router.py`, behind `AuthDep`.
23. Specification: required task not marked complete: T8 — Backend tests: `test_knowledge_service.py`, `test_knowledge_api.py`, migration up/down/up test, and non-regression extensions to existing Notes/Documents/Secrets/Search test coverage.
24. Specification: required task not marked complete: T9 — Frontend data layer: `features/knowledge/api.ts` plus the three TanStack Query hooks and invalidation rules from `05_COMPONENTS.md` §4.
25. Specification: required task not marked complete: T10 — Frontend Hub page: `app/(app)/knowledge/page.tsx` and the filter bar / result list / result row components; all five states, four-parameter URL sync (no page/offset), the fixed truncation notice, responsive layout.
26. Specification: required task not marked complete: T11 — Frontend tag & link controls: the tag picker and link panel/picker, mounted into the Documents sidebar and the Notes card surface; resolves the sticky-note layout question and the `select.tsx` workaround.
27. Specification: required task not marked complete: T12 — Navigation: one new `NavItem` in `lib/nav-registry.ts`; command-palette pickup is automatic.
28. Specification: required task not marked complete: T13 — Accessibility pass: keyboard operation, roles/names, the `aria-live` result region, focus indicators in both themes.
29. Specification: required task not marked complete: T14 — Regression pass: full backend suite, `tools/tests`, lint, typecheck, production build, `docker compose build`; confirms no new dependency and no outbound call/credential material introduced.
30. Specification: required task not marked complete: T15 — Manual browser QA: the full `07_TESTING.md` §3 script, including the Ingest→Note→Hub round trip and the `/search`/⌘K non-regression check.
31. Specification: required task not marked complete: T16 — Documentation & final state: `docs/API.md`, `docs/Database.md`, `docs/Architecture.md` (if applicable), `02_ROADMAP.md`, `forge-docs/implementation/README.md`, this phase's `CURRENT_STATE.md`/`README.md`; confirms frozen phases and `tools/fdk_verification/` untouched and the Definition of Done checklist satisfied.
32. Specification: acceptance criterion not marked complete: AC1 (FR1). `/knowledge` lists both Notes and Documents in one list. With N notes and M documents present, the Hub shows N+M items, each carrying a type badge that matches its source, a title (or "Untitled"), an excerpt, its tags, its project, and a last-updated time.
33. Specification: acceptance criterion not marked complete: AC2 (FR2). A query matching only a note's content returns that note; a query matching only a document's content returns that document; a query matching both returns both. A query matching neither returns zero items and the filtered-empty state.
34. Specification: acceptance criterion not marked complete: AC3 (FR2, §6.4). No new FTS index or content table exists. Verified by inspecting migration `0009_knowledge_hub.py` for any `fts` DDL (there must be none) and by confirming `services/knowledge/service.py` calls `notes_service.search_notes` / `documents_service.search_documents` rather than issuing its own `MATCH` SQL.
35. Specification: acceptance criterion not marked complete: AC4 (FR3). `type=note` returns only notes; `type=document` returns only documents; `type=all` returns both.
36. Specification: acceptance criterion not marked complete: AC5 (FR4, §6.8). Filtering by one tag returns exactly the items carrying it. Filtering by two tags returns only items carrying both (AND) — a fixture item carrying just one of the two selected tags is excluded. No request or UI control produces OR results; this is the only mode.
37. Specification: acceptance criterion not marked complete: AC6 (FR5). Filtering by a project returns only items whose `project_id` matches; unscoped items are excluded. No new scoping column was added (verified against `04_DATABASE.md` §1 — three new tables, no `ALTER TABLE`).
38. Specification: acceptance criterion not marked complete: AC7 (FR6). A query, a type, a tag set, and a project applied simultaneously return the intersection. With no query, results are ordered by `updated_at` descending.
39. Specification: acceptance criterion not marked complete: AC8 (FR7). Activating a note result navigates to `/notes`; activating a document result navigates to `/documents`. The Hub renders no note or document editor of its own. Per-item deep linking is best-effort only — neither feature has a per-item route today, and adding one is out of scope; landing on the correct page is what this criterion requires.
40. Specification: acceptance criterion not marked complete: AC8a (FR8a, §6.6/§6.7). `GET /api/knowledge` returns at most 100 items, ordered by `updated_at` descending before the cap is applied. With 105 matching items, the response contains exactly 100 items, `total: 105`, `truncated: true`, and the 100 returned are the most recently updated (the 5 oldest matches are excluded, not an arbitrary 5). `GET /api/knowledge` accepts no `page`, `offset`, `cursor`, or `limit` parameter — the cap is a fixed server constant, not caller-configurable, and no other Phase 07 endpoint accepts one either.
41. Specification: acceptance criterion not marked complete: AC9 (FR8). Four distinct states are observable: loading skeleton; "no knowledge at all" empty state; "no items match these filters" empty state with a clear-filters action; populated list. The two empty states are visibly different.
42. Specification: acceptance criterion not marked complete: AC10 (FR9–FR10). A tag can be assigned to a Note and to a Document from their own editing surfaces. Assigning a brand-new name creates one `tags` row; assigning an existing name reuses that row — after both operations the `tags` table contains exactly one row for that name.
43. Specification: acceptance criterion not marked complete: AC11 (FR11). Removing a tag from an item removes only the assignment; the `Tag` row still exists and remains assigned to any other item that had it.
44. Specification: acceptance criterion not marked complete: AC12 (FR12). `GET /api/knowledge/tags` returns only tags assigned to at least one Note or Document, each with a count equal to the number of matching items. A tag assigned only to a Secret does not appear.
45. Specification: acceptance criterion not marked complete: AC13 (FR13). After assigning knowledge tags, every Secrets endpoint returns identical responses to before, and `secret_tag_links` is unchanged. Verified by the existing Secrets test suite passing unmodified plus the explicit FR13 test.
46. Specification: acceptance criterion not marked complete: AC14 (FR14). Links can be created note→note, note→document, and document→document. All three combinations persist and are retrievable.
47. Specification: acceptance criterion not marked complete: AC15 (FR15). A link created from item A to item B appears in both A's and B's linked-items lists, with the `other` field correctly identifying the opposite item from each side.
48. Specification: acceptance criterion not marked complete: AC16 (FR16). Deleting a link from either side removes it from both sides. Both items still exist and are unchanged.
49. Specification: acceptance criterion not marked complete: AC17 (FR17). Deleting a Note or Document removes every `knowledge_links` row referencing it from either the source or target side. A direct database query for links referencing the deleted id returns zero rows.
50. Specification: acceptance criterion not marked complete: AC18 (FR18). Linking an item to itself returns `400 knowledge_link_self`. Creating a link that already exists — in either direction — returns `409 knowledge_link_duplicate`. Neither is silently ignored.
51. Specification: acceptance criterion not marked complete: AC19 (FR19). Knowledge Hub appears in the sidebar and is reachable from the ⌘K command palette.
52. Specification: acceptance criterion not marked complete: AC20 (FR20). Converting a file via Ingest and saving it as a Note produces an item that appears in the Hub, is taggable, and is linkable. Ingest gained no database table and no new endpoint.
53. Specification: acceptance criterion not marked complete: AC21 (FR21). `GET /api/search` returns the same response shape and content as before this phase, `/search` behaves identically, and the ⌘K palette still returns Secrets results. Verified by the explicit response-shape test plus the existing search tests passing unmodified. ADR-0007 is unmodified.
54. Specification: acceptance criterion not marked complete: AC22. Every screen state in `02_UI.md` §3 is present and reachable: Hub loading, both empty states, error-with-retry, populated; tag control loading/empty/populated; linked-items empty/populated.
55. Specification: acceptance criterion not marked complete: AC23. Self-link and duplicate-link errors render inline next to the control, not as a transient toast that disappears before it can be read.
56. Specification: acceptance criterion not marked complete: AC24. Whenever the API returns `truncated: true`, the Hub shows "Showing the first 100 of N — narrow your filters" (N is the real `total`). When `truncated: false`, no truncation notice appears at all. No result set is silently cut, and no "load more" / "next page" / page-number control exists anywhere on the Hub — this phase has no pagination.
57. Specification: acceptance criterion not marked complete: AC25. Active filters (`q`, `type`, `tag_id`, `project_id`) are encoded in the URL; reloading a filtered Hub URL restores exactly that filtered view. No page/offset parameter is ever part of the URL, because none exists.
58. Specification: acceptance criterion not marked complete: AC26. Every new interactive element — query input, type selector, tag multi-select, project selector, result rows, tag chips and their remove buttons, link picker, link remove — is reachable and operable by keyboard alone, with a visible focus indicator.
59. Specification: acceptance criterion not marked complete: AC27. The result count / truncation notice is inside an `aria-live="polite"` region and announces when filtering changes the result set. Tag chip remove buttons have accessible names of the form "Remove tag <name>".
60. Specification: acceptance criterion not marked complete: AC28. All new surfaces render correctly in light and dark theme and remain usable at mobile viewport widths, with the result list stacking rather than scrolling horizontally.
61. Specification: acceptance criterion not marked complete: AC29. All tests in `07_TESTING.md` pass — the full backend suite (`backend/.venv/Scripts/python.exe -m pytest backend/tests`) with the new `test_knowledge_service.py` and `test_knowledge_api.py`, and the migration up/down/up test.
62. Specification: acceptance criterion not marked complete: AC30. `npm --prefix frontend run lint`, `frontend/node_modules/.bin/tsc --noEmit`, `npm --prefix frontend run build`, and `docker compose build` all exit 0.
63. Specification: acceptance criterion not marked complete: AC31. No architectural invariant in `03_ARCHITECTURE.md` §2 is violated: routers stay thin, no cross-feature import between `notes` and `documents` was introduced, models remain the schema source of truth, and the schema change ships as explicit migration `0009_knowledge_hub.py`.
64. Specification: acceptance criterion not marked complete: AC32. No new external dependency was added — `frontend/package.json` and `backend/requirements.txt` are unchanged, and `06_TECH_STACK.md` needs no new entry.
65. Specification: acceptance criterion not marked complete: AC33. The phase adds zero outbound network calls and stores no credential material. Verified by code review of `services/knowledge/` and a grep for `httpx` / `requests` / `api_key` / `encrypted_` in the new module returning nothing.
66. Specification: acceptance criterion not marked complete: AC34. `08_DEFINITION_OF_DONE.md`'s feature-level checklist is satisfied.
67. Specification: acceptance criterion not marked complete: AC35. The FDK verification tooling is untouched and still green: `backend/.venv/Scripts/python.exe -m pytest tools/tests` passes, and `git diff` shows no change under `tools/fdk_verification/` or `tools/tests/`.
68. Specification: acceptance criterion not marked complete: AC36. Frozen phases are unmodified: `git diff` shows no change under `forge-docs/implementation/Phase-05-Model-Playground/` or `Phase-06-Projects/`, and no change to `forge-docs/decisions/0007-search-dedicated-page.md`.
69. Specification: acceptance criterion not marked complete: AC37. `docs/API.md` documents all seven new endpoints from `06_API.md` §1.
70. Specification: acceptance criterion not marked complete: AC38. `docs/Database.md` documents the three new tables, including the polymorphic-link integrity trade-off from `04_DATABASE.md` §2.
71. Specification: acceptance criterion not marked complete: AC39. `CURRENT_STATE.md` reflects reality with no stale "In Progress" items, and `README.md`'s Definition of Complete is satisfied.
72. Specification: acceptance criterion not marked complete: AC40. `02_ROADMAP.md` and `forge-docs/implementation/README.md` show Phase 07's correct status.
73. Test: Test backend ('backend/.venv/Scripts/python.exe -m pytest backend/tests -q') exited 1: 'backend' is not recognized as an internal or external command,
operable program or batch file.

74. Test: Test FDK tooling regression ('backend/.venv/Scripts/python.exe -m pytest tools/tests -q') exited 1: 'backend' is not recognized as an internal or external command,
operable program or batch file.
